query($cursor: String, $filter: IssueFilters, $order: IssueOrder) {
  repository(owner:"ethereum", name:"go-ethereum") {
    issues(first:20, after: $cursor, filterBy: $filter, orderBy:$order) {
  		nodes {
        title
        body
      }
      pageInfo {
        #startCursor
        endCursor
        hasNextPage
      }
    }
  }
}
